import 'package:flutter/material.dart';

class CustomerRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Customer Request')),
    );
  }
}